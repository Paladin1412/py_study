function GLOBAL_resize() {
    /*//获取body高度
    var body_height = $("body").height();
    //获取header高度
    var header_height = $(".omp-header").height();
    //获取wrapper高度
    var wrapper_height = $(".omp-wrapper").height();
    //获取
    //获取最大高度
    var max_height = body_height > wrapper_height ? body_height : wrapper_height;
    //设置main / menu高度
    var auto_height = max_height - header_height;
    $(".omp-menu").height(auto_height + 40);
    //footer位置 40px为宽度
    $(".omp-footer").css("top", max_height);*/
}

/*$(window).resize(function(event) {
    GLOBAL_resize();
});

$(window).load(GLOBAL_resize);
*/